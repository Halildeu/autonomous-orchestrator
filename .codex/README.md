# Repo-local Codex config

This folder contains the SSOT Codex config for this repo. It is deterministic and
contains no secrets.

Set `CODEX_HOME` to this directory to use the repo-local config instead of a
global config.
